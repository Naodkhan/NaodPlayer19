# NAOD PLAYER — GitHub APK Build

1. ይህን project በGitHub repository ውስጥ አስገባ።
2. `Actions` → `Build NAOD PLAYER APK` ሂድ።
3. `Run workflow` ተጫን።
4. Build ከጨረሰ በኋላ workflow run ክፈት።
5. `Artifacts` ስር `NAOD-PLAYER-debug-APK` አውርድ።
6. ZIP ን ከፍተህ `app-debug.apk` ን በAndroid ላይ install አድርግ።

Push ብታደርግ `main` ወይም `master` ላይ ራሱ build ይጀምራል።
